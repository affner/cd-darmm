spring:
  datasource:
    url: jdbc:sqlite:cwdarmm.db
    driver-class-name: org.sqlite.JDBC

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    # Aquí solamente el FQCN, sin citas ni espacios adicionales:
    database-platform: org.hibernate.community.dialect.SQLiteDialect

